/**
 * Default values are declared here
 * Default import is a JSON of all the values
 */
export const defaultJWTSecret: string = 'verysecret';

export default {
    defaultJWTSecret: defaultJWTSecret
}
